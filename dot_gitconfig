[include]
	path = ~/.gitconfig.drowning-cat
[includeIf "gitdir:Projects/rss/"]
	path = ~/.gitconfig.lazy-goose

[init]
	defaultBranch = main

[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
	line-numbers = true
[merge]
	conflictStyle = zdiff3
